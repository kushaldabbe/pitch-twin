"""Data contract: schema validation + synthetic data generation.

This is the single coupling point between the Python CV producer and the JS viewer.
See ``schemas/pitchtwin.frame.v1.json`` and DESIGN.md section 5.
"""
